import React, { Component } from 'react';
import logo from './logo.svg';
import '.Musician.css';

class Musician extends Component {
    render() {
        return(
            <img src={logo} alt={musician.name}/>
        );
    }
}